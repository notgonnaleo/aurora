@using Backend.Domain.Entities.Authentication.Tenants;
@using Backend.Domain.Entities.Authentication.Users.UserContext
@using Microsoft.JSInterop;
@inject IJSRuntime JSRuntime

@if (Profile is not null && Profile.Tenant is not null)
{
    <div class="card card-body bg-primary" style="height: 205px;">
        <h5 class="card-title text-light fw-semibold mb-4">
            <strong>Bem vindo, @Profile?.Username!</strong>
        </h5>
        @if (IsAuthenticated)
        {
            <p class="text-light mb-0">Você está logado na empresa: <strong>@Profile!.Tenant!.Name</strong></p>
        }
    </div>
}

@code
{
    [Parameter] public bool IsAuthenticated { get; set; }
    [Parameter] public UserSessionContext? Profile { get; set; }
}
